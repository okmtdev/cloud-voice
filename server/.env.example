# Cloud Voice relay server configuration. Copy to `.env` for local development.

# Port the relay listens on (Cloud Run injects this automatically in prod).
PORT=8080

# Optional shared secret. When set, every WebSocket connection (agent + web)
# must present a matching `token`. Leave empty to disable auth locally.
CLOUD_VOICE_TOKEN=
